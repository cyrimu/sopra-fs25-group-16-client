const submitButton: React.FC = () => {
  return (
    <div
      style={{
        padding: "12px 18px",
        color: "#000",
        fontWeight: 700,
        borderRadius: "50px",
        fontFamily: "Gabarito",
        backgroundColor: "#fff",
      }}
    >
      Submit Selection
    </div>
  );
};

export default submitButton;
